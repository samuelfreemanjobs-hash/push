# 01 — Clone and env

See `SETUP.md` Step 1.

Quick commands:

```bash
cp .env.example .env.local
npm install
npm run dev
```

Fill app branding vars first. Supabase and Lemon Squeezy keys come in the next steps.
